---

## 🧾 `.gitignore` 建議內容

```gitignore
# Byte-compiled / cache
__pycache__/
*.py[cod]

# Editor settings
.vscode/
.idea/

# System files
.DS_Store
Thumbs.db

# SQLite database
*.db

# Build artifacts
*.deb
*.tar.gz
*.zip
dist/
build/
